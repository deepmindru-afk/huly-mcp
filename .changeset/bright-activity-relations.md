---
"@firfi/huly-mcp": minor
---

Improve LLM-first discovery for activity and issue relations: `list_activity` now accepts issue, document, and channel identifiers, and `list_issue_relations` now returns both `blockedBy` and `blocks`.
