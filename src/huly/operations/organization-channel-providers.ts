/* eslint-disable import-x/no-unused-modules -- compatibility aliases are intentionally exported for older imports. */
export {
  CONTACT_CHANNEL_PROVIDER_BY_SDK_KEY as ORGANIZATION_CHANNEL_PROVIDER_BY_SDK_KEY,
  fromContactChannelProviderRef,
  toContactChannelProviderRef as toChannelProviderRef
} from "./contact-channel-providers.js"
